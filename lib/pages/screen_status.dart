import 'package:flutter/material.dart';

class StatusScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
        "Status",
        style: new TextStyle(fontSize: 24.0),
      ),
    );
  }
}